#include <stdio.h>
#include <stdlib.h>
int main(int argc, char **argv) {
    for (int i = 1; i < argc; ++i) {
        FILE *file = fopen(, const char *restrict mode)
        fprintf(stdout, "");
    }
    exit(EXIT_SUCCESS);
}   